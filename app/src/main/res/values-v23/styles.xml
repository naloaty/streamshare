<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- SyncShare themes -->
    <style name="Theme.SyncShare.Light.NoActionBar" parent="Theme.SyncShare.Base.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/white</item>

        <item name="android:windowLightStatusBar">true</item>

        <item name="android:windowTranslucentNavigation">true</item>

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="Theme.SyncShare.Dark.NoActionBar" parent="Theme.SyncShare.Base.Dark.NoActionBar">
        <item name="android:statusBarColor">@color/colorBlackTransparent</item>
        <item name="android:navigationBarColor">@color/colorBlackTransparent</item>

        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

</resources>